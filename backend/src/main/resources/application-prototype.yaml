swagger:
  server:
    url: "https://testphase.rechtsinformationen.bund.de/"

spring:
  config:
    import:
      - "kubernetes:"
      - "optional:configtree:/etc/secrets/*/"

s3:
  file-storage:
    case-law:
      versionPrefix: "V1/"
