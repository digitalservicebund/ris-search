server:
  front-end-url: "https://ris-portal-staging.dev.tech.digitalservice.dev/"
  cms-url: "https://ris-cm.staging.tech.digitalservice.dev/"

swagger:
  server:
    url: "https://ris-portal-staging.dev.tech.digitalservice.dev/"

spring:
  config:
    import:
      - "kubernetes:"
      - "optional:configtree:/etc/secrets/*/"

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus

s3:
  file-storage:
    case-law:
      versionPrefix: "V1/"
